// daily_sales/pages/edit_daily_sales_report_page.dart

import 'package:flutter/material.dart';

class EditDailySalesReportPage extends StatelessWidget {
  const EditDailySalesReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Daily Sales Report')),

      body: const Center(child: Text('Edit Report Page')),
    );
  }
}
